SELECT p.user_id, p.start_date, (

                    SELECT COUNT(*)
                    FROM users u
                    WHERE p.start_date>u.start_date
  									GROUP BY u.user_id
                    ORDER BY u.start_date
  ) AS users_before 
  FROM users p
  ORDER BY p.start_date ASC
  
  
